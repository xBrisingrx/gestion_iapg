<h1>Sedes</h1>
<div class="row justify-content-between mb-2">
  <div class="col-4">
    <% if policy(@headquarters).edit? %>
      <%= link_to "Registrar sede", 
        new_headquarter_path,
        data: { turbo_frame: "modal"},
        class: "btn btn-sm btn-primary" %>
    <% end %>
  </div>
  <div class="col-6 col-md-4 col-lg-3">
    <%= search_form_for @query, method: :get, data: { controller: "filter", filter_target: "form", turbo_frame: "table" } do |form| %>
      <div class="input-group input-group-sm">
        <span class="input-group-text">
          <i class="bi bi-search"></i>
        </span>
        <%= form.search_field :name_or_description_or_sectional_name_or_city_name_cont,
          placeholder: "Filtrar...",
          class: "form-control form-control-sm",
          autocomplete: "off",
          data: { action: "input->filter#search" }
        %>
      </div>
    <% end %>
  </div>
</div>
<%= turbo_frame_tag 'table', id: 'headquarters_table' do %>
  <div class="table-responsive">
    <table class="table table-hover">
      <thead class="thead-light">
        <tr>
          <th><%= sort_link(@query, :name, "Nombre") %></th>
          <th><%= sort_link(@query, :description, "Descripción") %></th>
          <th><%= sort_link(@query, :can_make_psychometric, "Realiza psicométrico") %></th>
          <th><%= sort_link(@query, :sectional_id, "Seccional") %></th>
          <th><%= sort_link(@query, :city_id, "Ubicación") %></th>
          <th><%= sort_link(@query, :city_id, "Ciudad") %></th>
          <th>Acciones</th>
        </tr>
      </thead>
      <tbody id='tbody_headquarters'>
        <%= render @headquarters %>
        <%= render(partial: "shared/not_found_data", locals: {colspan: 7}) if @headquarters.empty?%>
      </tbody>
    </table>
    <%== pagy_bootstrap_nav(@pagy) %>
  </div>
<% end %>
