<%= turbo_frame_tag "form_images" do %>
    <%= form_with url: upload_multiple_images_people_path, local: true, multipart: true, builder: BootstrapBuilder do |f| %>
    <%= f.file_field :zip_file %>
    <%= f.submit %>
    <% end %>
<% end %>