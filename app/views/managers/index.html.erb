<p style="color: green"><%= notice %></p>

<% content_for :title, "Managers" %>

<h1>Managers</h1>

<div id="managers">
  <% @managers.each do |manager| %>
    <%= render manager %>
    <p>
      <%= link_to "Show this manager", manager %>
    </p>
  <% end %>
</div>

<%= link_to "New manager", new_manager_path %>
